test.user.name: aaa
test:
  user:
    name: hello nldc-gateway
eureka:
  client:
    healthcheck:
      enabled: true
zuul:
  routes:
    gateway:
      path: /gateway/**
      service-id: user-controller
